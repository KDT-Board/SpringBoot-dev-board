package com.kdt.board.jwt;

/**
 * JWT 관련 상수를 정의하는 클래스
 */
public class JwtConstants {
    public static final String JWT_HEADER = "Authorization"; // 요청 헤더에서 JWT를 가져올 때 사용
    public static final String JWT_PREFIX = "Bearer "; // JWT의 접두사
}
